/* $Id: port.h.in,v 8.1 1996/02/18 15:37:08 bostic Exp $ (Berkeley) $Date: 1996/02/18 15:37:08 $ */

/* Declare the basic types, if not already declared. */
@u_int8_decl@
@int16_decl@
@u_int16_decl@
@int32_decl@
@u_int32_decl@

/* 
 * XXX
 * If we don't have mmap, we fake it with read and write.
 */
#ifndef HAVE_MMAP
#define	PROT_READ	0x1	/* pages can be read */
#define	PROT_WRITE	0x2	/* pages can be written */
#define	PROT_EXEC	0x4	/* pages can be executed */

#define	MAP_SHARED	1	/* share changes */
#define	MAP_PRIVATE	2	/* changes are private */
#endif

/*
 * XXX
 * Maximum path length.
 */
#ifndef MAXPATHLEN
#ifdef PATH_MAX
#define	MAXPATHLEN	PATH_MAX
#else
#define	MAXPATHLEN	1024
#endif
#endif

/*
 * XXX
 * DB isn't always portable, and we want the private information.
 */
#define __DBINTERFACE_PRIVATE
#define DB      __DB

/*
 * XXX
 * Handle function prototypes and the keyword "const".  This steps on name
 * space that DB doesn't control, but all of the other solutions are worse.
 */
#undef	__P
#if defined(__STDC__) || defined(__cplusplus)
#define	__P(protos)	protos		/* ANSI C prototypes */
#else
#define	const
#define	__P(protos)	()		/* K&R C preprocessor */
#endif

/* Define the paths. */
#define	_PATH_BSHELL	"@vi_cv_path_shell@"
#define	_PATH_EXRC	".exrc"
#define	_PATH_MSGCAT	"./"
#define	_PATH_NEXRC	".nexrc"
#define	_PATH_PRESERVE	"/var/tmp/vi.recover"
#define	_PATH_SENDMAIL	"@vi_cv_path_sendmail@"
#define	_PATH_SYSEXRC	"/etc/vi.exrc"
#define	_PATH_TAGS	"tags"
#ifndef _PATH_TMP
#define	_PATH_TMP	"/tmp"
#endif
#ifndef _PATH_TTY
#define	_PATH_TTY	"/dev/tty"
#endif
